[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
# Keep migrate + start together, but echo failures clearly in Deploy Logs
startCommand = "npx prisma migrate deploy && npm run start"
restartPolicyType = "ON_FAILURE"
